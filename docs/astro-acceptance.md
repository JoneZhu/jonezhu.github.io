# Astro 改版验收记录

验收日期：2026-09-17。Pi agent 负责产品实现，Codex 负责设计、代码审查与独立验收。

## 内容与兼容性

- 构建生成 277 个 HTML 页面；旧 Hexo 基准的 263 个 HTML 地址全部存在。
- 127 篇文章进入文章列表与搜索，搜索 URL 无重复；草稿未公开。
- 迁移前记录的 297 个原始 source 文件 SHA-256 全部保持一致。
- 旧站 168 个静态资源路径全部保留，包括历史附件别名。
- 全量核对旧正文标题锚点：无遗漏；文章页没有重复 ID。
- 所有页面均为中文语言标记，且只有一个 h1。
- sitemap 的 211 个地址均存在且可索引，RSS XML 可以解析。
- 全站内部链接扫描仅留下旧文原有的 `../lib/providers/gimbal_provider.dart` 源码引用，未捏造下载文件。另一个同域项目 `/CC-Parser/` 已独立请求确认 HTTP 200。

## 浏览器验收

在实际 Chromium 浏览器中检查构建产物，而非只检查开发模板：

- 首页、文章列表、最后一页、归档、年份归档、技术分类、关于、404、长技术文、图文文章，分别检查 1440px、390px、320px 宽度：没有页面横向溢出。
- 首页桌面最新文章区域位于约 720px，首屏可见；手机精选压缩摘要，保留清晰标题。
- 中文搜索“云台”返回 8 篇文章；代码符号 `SubscriptionService` 返回对应 Provider 文章；无结果、清空状态正确。
- 深色模式切换后刷新仍保持选择。
- 代码复制显示“已复制”，剪贴板内容与代码原文一致。
- 手机自动目录默认折叠；桌面展开；章节跳转可以找到真实目标。
- 检视浅色桌面与手机首页、归档，以及深色阅读和搜索页面截图。

## 写作与构建

- `npm ci` 安装成功，独立 `npm run build` 成功。
- `npm audit --omit=dev`：0 vulnerabilities。
- 新文章命令在临时目录验证：特殊字符标题完整保留、文件名安全转换、重复文件拒绝覆盖、换行标题被拒绝、草稿写入正确目录。
- README 说明 Markdown 写作、图片路径、精选配置、草稿、Astro 开发服务管理和 GitHub Pages 发布流程。
- GitHub Actions 使用 Node 22、锁文件安装和 `dist/` 发布。

## 第三方评论限制

utterances 保留 `JoneZhu/github-page-comment` 与 `issue-term=pathname`。修正 iframe 就绪前发送主题消息的竞态；验收中未再出现该 origin 警告。但本地浏览器请求 GitHub 评论 API 时出现 `ERR_NETWORK_CHANGED`，因此不能声称真实评论内容和 iframe 内主题切换已完整通过。页面提供“在 GitHub 查看讨论”的直接入口，评论内容仍取决于第三方服务可达性。
